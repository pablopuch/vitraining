---
import { siAppstore, siGoogleplay } from "simple-icons";

const appLinks = {
    appStore:
        "https://apps.apple.com/es/app/vitraining/id6467152259?platform=iphone",
    googlePlay:
        "https://play.google.com/store/apps/details?id=com.weappin.vitraining",
};
---

<section class="py-16 md:py-20 bg-gray-900 text-center">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-white text-3xl md:text-4xl font-bold mb-4">
            Gestiona tus clases desde nuestra App
        </h2>
        <p class="text-gray-300 text-lg md:text-xl mb-8 max-w-2xl mx-auto">
            Reserva tus sesiones, sigue tu progreso y mantente al día de todas
            las novedades directamente desde tu móvil.
        </p>
        <div class="flex justify-center items-center gap-4 md:gap-6 flex-wrap">
            <a
                href={appLinks.googlePlay}
                target="_blank"
                rel="noopener noreferrer"
                class="badge-button"
            >
                <Fragment set:html={siGoogleplay.svg} />
                <span class="text-container">
                    <span class="text-xs">DISPONIBLE EN</span>
                    <span class="text-lg font-semibold">Google Play</span>
                </span>
            </a>
            <a
                href={appLinks.appStore}
                target="_blank"
                rel="noopener noreferrer"
                class="badge-button"
            >
                <Fragment set:html={siAppstore.svg} />
                <span class="text-container">
                    <span class="text-xs">Descárgalo en la</span>
                    <span class="text-lg font-semibold">App Store</span>
                </span>
            </a>
        </div>
    </div>
</section>

<style>
    .badge-button {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        background-color: white;
        color: black;
        padding: 0.5rem 1.5rem;
        border-radius: 0.75rem;
        transition: transform 0.3s;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        text-align: left;
        min-width: 210px;
        justify-content: center;
    }
    .badge-button:hover {
        transform: scale(1.05);
    }
    .badge-button :global(svg) {
        height: 2rem;
        width: 2rem;
        fill: currentColor;
    }
    .badge-button .text-container {
        display: flex;
        flex-direction: column;
        line-height: 1.2;
    }
    .badge-button .text-xs {
        font-size: 0.7rem;
        font-weight: 400;
        text-transform: uppercase;
    }
    .badge-button .text-lg {
        font-size: 1.125rem;
    }
</style>
